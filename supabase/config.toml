# bal24 v2 — Supabase 프로젝트 설정 (Edge Function JWT 검증 정책 명시)
# SECURITY-EDGE-FUNCTION-AUTH — anon JWT 만으로는 호출 불가하도록 verify_jwt: true 명시

project_id = "clsljkxvgmqwenettkrz"

[functions.ai-chat]
verify_jwt = true

[functions.analyze-survey]
verify_jwt = true

[functions.send-invite]
verify_jwt = true

[functions.send-notification]
verify_jwt = true

[functions.create-member]
verify_jwt = true

# 박경수님 2026-05-26 STEP-STAFF-PORTAL-PIN-GATEWAY — 비로그인(/portal) 호출 함수.
# 비로그인 강사가 이름·PIN 으로 인증하므로 JWT 검증을 꺼야 함.
# 보안은 함수 본문 자체 검증 (이름+PIN 매칭 실패 시 401, 3회 잠금) + SERVICE_ROLE 로 처리.
[functions.verify-staff-pin]
verify_jwt = false

[functions.change-staff-pin]
verify_jwt = false

# 박경수님 2026-05-26 STEP-PORTAL-MULTI-FIX PART F — 비로그인 강사가 AI 일지 생성 요청.
[functions.mentoring-log-ai]
verify_jwt = false
